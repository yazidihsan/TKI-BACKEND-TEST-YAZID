spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/crud_db
    username: postgres
    password: naDnaD1428
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  data:
    mongodb:
      uri: mongodb://localhost:27017/crud_db
